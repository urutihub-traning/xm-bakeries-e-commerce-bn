export const TestController = (req, res) => {
  res.json({ message: "test complete successful" });
};

export const exampleController = (req, res) => {
  res.json({ message: "this is example api" });
}